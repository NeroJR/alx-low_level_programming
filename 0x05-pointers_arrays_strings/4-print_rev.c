#include "main.h"

/**
 *print_rev - prints string in reverse
 *@s: parameter
 */

void print_rev(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
		continue;

	for (i--; i >= 0; i--)
		_putchar(s[x]);

	_putchar('\n');
}
